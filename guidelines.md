# Guidelines

Load this file for TypeScript style policy, then load only the guideline pages needed for the task.

Guideline pages are policy. Do not load every guideline page by default.

No guideline pages have been drafted yet. As pages land under `guidelines/`, list each one here under a section heading with a one-line description of when to load it.
